# YADOKARI Handoff - 2026-05-26

## Current Production State

- Production URL: https://yadokari-minpaku.jp
- Latest deployed commit in this note is historical; check `git log -1` for the newest commit before deploying.
- Recent production verification:
  - `npm run predeploy` passed locally.
  - Vercel production deploy completed.
  - `SMOKE_BASE_URL=https://yadokari-minpaku.jp npm run smoke` passed.
  - `/admin/listings` returned `200 OK`.
  - `/api/admin/listings` correctly returned `401` without login.
- Git working tree is clean except unrelated untracked `videos/`.

## What Was Finished

- Added server-side check usage limits for `/api/check-minpaku`.
- Fixed production signup/auth redirect flow.
- Added `/auth/complete`.
- Fixed Supabase email/header issue.
- Added external minpaku property link flow.
- Added official listing/map direction.
- Secured property listing submission:
  - Login required.
  - Pro plan required.
  - New submissions are stored as `PENDING`.
- Added admin review surface:
  - `/admin/listings`
  - `GET /api/admin/listings`
  - `PATCH /api/admin/listings/[id]`
  - Admin access uses `ADMIN_EMAILS`, falling back to `CONTACT_EMAIL`.
- Added admin-only dashboard entry:
  - `/api/admin/check`
  - `/dashboard` shows the listing review link only for admin users.
- Added billing management:
  - `/billing`
  - `GET /api/billing`
  - `POST /api/billing-portal`
  - Paid users can open Stripe Billing Portal when `stripeCustomerId` exists.
- Added approved listing display plumbing:
  - `/api/listings` returns `ACTIVE` listings only when `NEXT_PUBLIC_PROPERTY_MARKETPLACE_ENABLED=true`.
  - `/api/listings/[id]/contact` exposes contact info only for logged-in users and only while the marketplace flag is enabled.
  - `/properties` renders approved listings above the external link collection when public listings are enabled.

## Important Routes

- User property candidate page: `/properties`
- Property listing application: `/submit-property`
- Admin listing review: `/admin/listings`
- Billing management: `/billing`
- Official minpaku listing map/list: `/listings` and `/map`
- Health check: `/api/health`

## Property Listing Operation

1. A logged-in Pro user submits a listing from `/submit-property`.
2. The listing is saved as `PENDING`.
3. Admin opens `/admin/listings`.
4. Admin reviews the content.
5. Admin clicks:
   - `公開する` -> `ACTIVE`
   - `却下する` -> `REJECTED`
   - `非公開` -> `EXPIRED`
6. Public APIs only expose `ACTIVE` listings.

## Environment Notes

Production already has core integrations configured:

- Supabase
- Database
- Stripe checkout
- Resend/email
- Google Analytics
- Vercel domain

Admin env:

```text
ADMIN_EMAILS=araki3312@gmail.com
```

If `ADMIN_EMAILS` is not set, admin access falls back to `CONTACT_EMAIL`.

Marketplace env:

```text
NEXT_PUBLIC_PROPERTY_MARKETPLACE_ENABLED=false
```

Keep this false until real reviewed listings are ready to show publicly.

## Next Recommended Tasks

1. Log in with the admin email and open `/dashboard`; confirm the admin-only `掲載申請管理` link appears.
2. Open `/admin/listings`; confirm `PENDING`, `ACTIVE`, `REJECTED`, and `EXPIRED` filters load.
3. Submit one test property from `/submit-property` using a Pro account.
4. Confirm it appears as `PENDING` in `/admin/listings`.
5. Approve it to `ACTIVE`.
6. Keep `NEXT_PUBLIC_PROPERTY_MARKETPLACE_ENABLED=false` until there is at least one real reviewed listing ready for public display.
7. When real listings are ready, set `NEXT_PUBLIC_PROPERTY_MARKETPLACE_ENABLED=true`, redeploy, and verify `/properties` shows the approved listing section.
8. For paid users, open `/billing` and confirm Stripe Billing Portal launches. If it does not, check that the user has `stripeCustomerId` from a completed Stripe Checkout webhook.

## Caution

- Do not stage or delete `videos/` unless it is intentionally part of a video deliverable.
- Do not expose fake/mock listings as real public inventory.
- Keep listing approvals manual until legal/contract checks are clear.
